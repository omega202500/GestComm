{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "phpVersion": "8.2"
    }
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}",
    "healthcheckPath": "/api/ping"
  }
}
